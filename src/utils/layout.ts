/**
 * Breakpoint único pra tela larga (desktop/notebook) — usado tanto pelo
 * conteúdo principal (padding pra não ficar embaixo do menu fixo) quanto pela
 * própria Sidebar (decide se fica fixa à esquerda ou como drawer deslizante).
 * Mesma árvore de componentes sempre; só o valor de estilo muda com a largura.
 */
export const DESKTOP_BREAKPOINT = 900;
export const SIDEBAR_DOCKED_WIDTH = 272;
